class .sht.cons.iStream.vtbl {
   printLn : ptr;
}

class .sht.cons.iStream {
   _vtbl : .sht.cons.iStream.vtbl;
}

func .sht.cons.iStream.ctor() : .sht.cons.iStream;
class .sht.cons.stdout.vtbl {
   printLn : ptr;
}

class .sht.cons.stdout {
   _vtbl : .sht.cons.stdout.vtbl;
}

func .sht.cons.stdout.ctor() : .sht.cons.stdout;

func .sht.cons.stdout.printLn(self : .sht.cons.stdout,
   msg : str) : void;
class .sht.cons.program.vtbl {
   run : ptr;
}

class .sht.cons.program {
   _vtbl : .sht.cons.program.vtbl;
   _out : .sht.cons.iStream;
}

func .sht.cons.program.ctor() : .sht.cons.program;
