interface lameBase {
   indexOpGet(i : ptr) : ptr;
   indexOpSet(i : ptr, val : ptr) : void;
}

class string : lameBase {

   override indexOpGet(i : ptr) : ptr
   {
   }

   override indexOpSet(i : ptr, val : ptr) : void
   {
   }

}
